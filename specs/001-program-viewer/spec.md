# Feature Specification: Просмотр программы тренировок

**Feature Branch**: `001-program-viewer`
**Created**: 2026-02-25
**Status**: Draft
**Input**: User description: "реализуем для начала супер простую версию, которая просто выводит всю программу пользователю в Telegram mini app. Вся программа должна быть легко редактируемой через админку, а при дальнейшем масштабировании и через интерфейс"

## Clarifications

### Session 2026-02-25

- Q: Как пользователь попадает в Mini App? → A: Через Telegram Bot (кнопка в боте открывает Mini App).
- Q: Как определяется «текущая неделя»? → A: Пользователь выбирает вручную, по умолчанию — неделя 1.
- Q: Как отображать упражнения без указания веса (например, Гиперэкстензия – 15×4)? → A: Добавлен 4-й тип нагрузки «собственный вес / без отягощения».

## User Scenarios & Testing *(mandatory)*

### User Story 1 — Просмотр программы тренировок (Priority: P1)

Пользователь нажимает кнопку в Telegram Bot, открывается Mini App.
Отображается программа тренировок на выбранную неделю (по умолчанию —
неделя 1), разбитая по дням. Для каждого упражнения отображаются
подходы, повторения и веса (в процентах, кг, с пометкой
индивидуального подбора или собственного веса). Пользователь может
переключаться между днями тренировок.

**Why this priority**: это ядро приложения — без отображения
программы остальные функции бессмысленны.

**Independent Test**: нажать кнопку в боте → увидеть программу
на неделю 1 → переключиться на другой день → убедиться,
что данные соответствуют эталонной PDF-программе.

**Acceptance Scenarios**:

1. **Given** пользователь нажал кнопку в Telegram Bot, **When**
   Mini App загружается, **Then** отображается программа тренировок
   на неделю 1 с разбивкой по дням (Пн, Ср, Пт).
2. **Given** пользователь видит программу, **When** он выбирает
   день (Пн/Ср/Пт), **Then** отображается список упражнений
   этого дня с подходами и повторениями.
3. **Given** упражнение имеет процентную нагрузку (например,
   `50%×6`), **Then** отображается процент от максимума,
   количество повторений и количество подходов.
4. **Given** упражнение имеет индивидуальный вес (🏋), **Then**
   отображается пометка об индивидуальном подборе веса
   с количеством повторений и подходов.
5. **Given** упражнение выполняется с собственным весом
   (например, `Гиперэкстензия – 15×4`), **Then** отображается
   только количество повторений и подходов без указания веса.

---

### User Story 2 — Управление программой через админку Django (Priority: P2)

Администратор входит в Django Admin и может создавать, редактировать
и удалять элементы программы тренировок: недели, дни, упражнения,
подходы. Изменения сразу отражаются в Mini App.

**Why this priority**: без возможности редактировать программу
контент будет захардкожен — это не масштабируется и затрудняет
обновление программы.

**Independent Test**: войти в Django Admin → изменить упражнение
(например, поменять процент) → открыть Mini App → убедиться,
что изменение отображается.

**Acceptance Scenarios**:

1. **Given** администратор вошёл в Django Admin, **When** он
   открывает раздел программы тренировок, **Then** он видит
   список недель с вложенными днями и упражнениями.
2. **Given** администратор редактирует упражнение, **When** он
   меняет параметры подхода (процент, повторения, подходы)
   и сохраняет, **Then** изменения сохраняются в базу данных.
3. **Given** администратор изменил программу, **When** пользователь
   открывает Mini App, **Then** отображается обновлённая
   программа.
4. **Given** администратор создаёт новую неделю, **When** он
   добавляет дни и упражнения, **Then** новая неделя появляется
   в Mini App.

---

### User Story 3 — Навигация по неделям программы (Priority: P3)

Пользователь может переключаться между неделями программы,
чтобы видеть план на любую неделю цикла. По умолчанию
отображается неделя 1.

**Why this priority**: для полноценного использования программы
нужно видеть не только текущую, но и предстоящие недели.

**Independent Test**: открыть Mini App → переключиться между
неделями → убедиться, что программа каждой недели отличается
и соответствует эталону.

**Acceptance Scenarios**:

1. **Given** пользователь видит программу, **When** он
   переключается на другую неделю, **Then** отображается
   программа выбранной недели.
2. **Given** в программе 4+ недели, **When** пользователь
   листает недели, **Then** навигация между ними работает
   плавно и без задержек.

---

### Edge Cases

- Что происходит, если программа пуста (администратор не добавил
  ни одной недели)? — Отображается сообщение «Программа пока
  не добавлена».
- Что происходит, если у дня нет упражнений? — День отображается,
  но с пометкой «День отдыха» или «Нет упражнений».
- Что происходит при медленном интернете? — Отображается индикатор
  загрузки, данные кэшируются после первой загрузки.
- Что если упражнение содержит несколько подходов через точку
  с запятой (например, `50%×6; 60%×5; 70%×4×2`)? —
  Все подходы отображаются последовательно.

## Requirements *(mandatory)*

### Functional Requirements

- **FR-001**: Система ДОЛЖНА отображать полную программу тренировок
  с разбивкой по неделям и дням.
- **FR-002**: Система ДОЛЖНА отображать для каждого упражнения:
  название, тип нагрузки (%, кг, индивидуальный, собственный вес),
  повторения и подходы.
- **FR-003**: Система ДОЛЖНА поддерживать нотацию программы:
  `%×повторения×подходы`, `🏋×повторения×подходы`,
  `кг×повторения×подходы` и `повторения×подходы` (собственный вес).
- **FR-004**: Система ДОЛЖНА предоставлять Django Admin интерфейс
  для CRUD-операций над программой (недели, дни, упражнения,
  подходы).
- **FR-005**: Изменения, внесённые через Django Admin, ДОЛЖНЫ
  быть доступны пользователям Mini App без перезапуска сервера.
- **FR-006**: Система ДОЛЖНА поддерживать навигацию между
  неделями и днями программы.
- **FR-007**: Система ДОЛЖНА корректно отображать упражнения
  с несколькими вариантами подходов (через точку с запятой).
- **FR-008**: Интерфейс ДОЛЖЕН быть на русском языке.
- **FR-009**: Точка входа — Telegram Bot с кнопкой открытия
  Mini App.
- **FR-010**: По умолчанию отображается неделя 1; пользователь
  выбирает неделю вручную.

### Key Entities

- **Неделя (Week)**: порядковый номер в программе, содержит
  тренировочные дни.
- **День (Day)**: день недели (Пн/Ср/Пт), принадлежит неделе,
  содержит упражнения.
- **Упражнение (Exercise)**: название (например, «Приседания»,
  «Жим лёжа»), категория (жим/присед/тяга/подсобка).
- **Подход (ExerciseSet)**: связь упражнения с днём, включает:
  тип нагрузки (процент/кг/индивидуальный/собственный вес),
  значение нагрузки (если применимо), количество повторений,
  количество подходов, порядок выполнения.

### Assumptions

- Программа тренировок загружается один раз из PDF и вносится
  через Django Admin.
- На данном этапе калькулятор 1ПМ не реализуется — отображаются
  только проценты и нотация из программы.
- Один пользователь видит одну общую программу (персонализация
  и расчёт индивидуальных весов — в следующих итерациях).
- Аутентификация пользователя через Telegram (initData) —
  стандартный механизм Telegram Mini App.

## Success Criteria *(mandatory)*

### Measurable Outcomes

- **SC-001**: Пользователь видит программу тренировок на текущую
  неделю за менее чем 3 секунды после открытия Mini App.
- **SC-002**: Администратор может добавить новую неделю
  с упражнениями за менее чем 5 минут через Django Admin.
- **SC-003**: 100% упражнений из эталонной PDF-программы
  корректно отображаются в Mini App (все подходы, повторения,
  нотация, включая упражнения с собственным весом).
- **SC-004**: Навигация между днями и неделями работает
  без задержки более 1 секунды.
